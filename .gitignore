# .gitignore - keeps installs, build output, generated caches and secrets out of the repository.

# Dependencies
node_modules/

# Build output
dist/

# Astro generated types and content cache
.astro/

# Cloudflare Wrangler local state
.wrangler/

# Variables d'environnement locales. Le theme n'en demande aucune pour
# construire ni pour se deployer ; ce fichier reste ignore par principe.
.env

# Secrets du Worker en local (moteur allume) : ALOHA_DEPLOY_HOOK s'y pose pour
# `astro dev`. En ligne, il se pose par `wrangler secret put`. Jamais versionne.
.dev.vars
.dev.vars.*

# Cartes de partage Open Graph : une photo seule, recadree depuis src/assets par
# scripts/og.mjs, que `pnpm build` lance juste apres covers.mjs. Refabriquees a
# chaque build, donc jamais versionnees.
public/og/

# Photographies de la demo : rapatriees par `pnpm covers`, refabriquees au build.
src/assets/covers/
src/assets/*.webp

# Outillage local des autres assistants de code : jamais publie.
# CLAUDE.md et .claude/settings.json, eux, font partie du produit : ils
# disent a l'agent de l'acheteur ou sont les regles de la maison et ce
# qu'il a le droit de lancer. Les conventions restent versionnees dans
# docs/conventions/.
.cursor/
.windsurf/

# Systeme et journaux.
.DS_Store
Thumbs.db
*.log

# Moteur de publication : etat local de D1 et R2, et types generes.
.emdash/
emdash-env.d.ts
