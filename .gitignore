# .gitignore - keeps installs, build output, generated caches and secrets out of the repository.

# Dependencies
node_modules/

# Build output
dist/

# Astro generated types and content cache
.astro/

# Cloudflare Wrangler local state
.wrangler/

# Local environment and secrets. Copy .env.example to .env, never commit .env.
.env

# Cartes Open Graph : generees par `pnpm og`, refabriquees au build.
public/og/

# Photographies de la demo : rapatriees par `pnpm covers`, refabriquees au build.
src/assets/covers/
src/assets/*.webp
